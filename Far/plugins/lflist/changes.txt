2016-12-14-0:
Если ни один файл не был отмечен, а курсор стоял в начале каталога, на
позиции двух точек (".."), то сканировался каталог выше,  относительно
текущего (например: если текущим каталогом был C:\1\2, то сканировался
каталог C:\1). Теперь в таком случае сканируются все файлы и  каталоги
на текущей панели.

2016-04-08-2:
Мелкие правки в справке.

2016-04-08-1:
Исправлена работа флага f для поля %N, теперь поле %fN работает
корректно.

Добавлен модификатор [s] для поля %D:

       Специальные последовательности:
           %[mr][s]D    имя директории

       Модификаторы
           m    нет меткиДиска
           r    Относительный путь
           S    с <D>: добавить в конец слэш, если значение не пусто


2016-04-08-0:
Добавлена опция "Исключить  промежуточные  каталоги",  исключающая  из
списка каталоги, имеющие подкаталоги.

Заменена опция "Не  включать  в  список  файлы  в  каталогах"  на  "Не
включать  в  список  файлы",  теперь  она  исключает  из  списка  даже
выделенные файлы.

2016-04-07-1:
Добавлены поля %<f,c,l,w>gT и  %<f,c,l,w>gY  для  вывода  GMT  времени
(доступа, записи, и т.д.) файла:

Исправлен вывод даты (поле %<f,c,l,w>Y), до этого выводилась GMT  дата
вместо локальной, из-за чего  вместо  даты  и  времени  записи  файла,
например 2016-04-03  01:00:00  для  EKT(GMT+5)  выдавалось  2016-04-02
01:00:00.

2016-04-07-0:
Добавлена опция "Не включать в список файлы в каталогах" по  просьбе с
форума: http://forum.farmanager.com/viewtopic.php?p=136927#p136927 .

2015-07-20-0:
Небольшой рефакторинг.

2015-07-08-1:
Добавлен расчёт размера каталогов, теперь поля  %S  и  %aS  показывают
суммарный размер каталога а не ноль, как было ранее.

Добавлена  сортировка  результата  по  полному  имени,  размеру,  дате
создания, дате модификации.

2015-07-08-0:
Изменён алгоритм парсинга имени файла, теперь если имя  файла/каталога
заканчиваются на точку, то точка остаётся частью имени файла.  Сделано
для любителей заканчивать имена каталогов многоточием.

Добавлен модификатор f для поля %N, выводящее полное имя для каталогов
(с расширением) и имя файла без расширения для некаталогов:

           f    с <N>: Для каталогов имя Файла.расширение Файла, иначе
                       имя Файла (без расширения)

2015-07-03-0:
Исправлена ошибка, из-за которой дата и  время  выводилось  без  учёта
часового пояса, и вместо минут выводились секунды.

2015-06-16-0:
Исправлена  ошибка,   приводившая   к   неправильному   сохранению   и
восстановлению данных

2014-09-19-5:
К результату шаблона %A (вывод атрибутов), добавлен еще один  атрибут:

                        D - Директория

2014-09-19-4:
Добавлен вывод текущего  обрабатываемого  каталога,  во  время  поиска
файлов. Теперь во время составления больших списков  и  длительных  по
времени вместо "зависших" панелей можно наблюдать какой сейчас каталог
сканируется... Хоть не так уныло... :-)

2014-09-19-3:
Исправлена ошибка формирования относительного пути файла, в результате
которой к относительному  пути  файла  добавлялся  каталог  файла  без
диска.

2014-09-19-2:
Отключен рекурсивный поиск в подкаталогах-связях (Reparse Point), т.к.
в Windows 7 при попытке составить список  файлов  папки  %USERPROFILE%
плагин натыкается на каталог-связь

  "%USERPROFILE%\AppData\Local\Application Data",

который  ссылается  на  "%USERPROFILE%\AppData\Local",  и   уходит   в
бесконечную рекурсию съедая при этом всю память и  выдавая  бесконечно
матюки на нехватку памяти. Теперь такие каталоги плагином видятся  как
пустые каталоги, со всеми вытекающими последствиями.

К результату шаблона %A (вывод атрибутов), добавлена ещё пара:

                        E - Точка повторной обработки
                        P - Разреженный

2014-09-19-1:
Если при вычислении относительных путей попадались магические  символы
функций поиска и замены, то относительный путь неправильно вычислялся.
Исправлено.

2014-09-19-0:
Пофиксено  отключение  рекурсивного  поиска,  при  выборе  пункта  "Не
включать в список каталоги с файлами"

2014-09-18-1:
Дополнена  работа  модификатора  r  (относительный  путь),  теперь  он
работает не только вглубь, но и вширь (и даже ввысь).  Например,  если
временная панель открыта из  каталога  "C:\Path\to\current\dir"  и  во
временной  панели  находится   файл   "C:\Path\to\file\file.txt",   то
относительный путь файла будет: "..\..\file\file.txt".

2014-09-18-1:
Исправление мелких ошибок в 2014-09-18-0.

2014-09-18-0:
Добавлен модификатор r для полей %P и %D, делающий путь  относительным
к текущему каталогу.

2014-09-17-0:
Рефакторинг.

Поправлены неправильные значения в  счётчиках  (%[osn]I)  в  первой  и
последней строке

Обозначение текущего каталога и каталога активной панели  в  первой  и
последней строке заменены с %P и %aP на %L и %aL.

Поля %L и %aL и теперь поддерживаются во всех трёх шаблонах.

Поправлена и дополнена справка.

2014-09-16-0:
Реорганизована загрузка/сохранение настроек, теперь  за  это  отвечает
отдельный модуль, возможна разовая пропажа настроек.

Содержимое функции export.Open перенесено  в  отдельный  модуль  main,
теперь export.Open только  запускает  одноименную  функцию  из  модуля
main. Таким образом реализация плагина отделена от интерфейса.

Небольшой рефакторинг.

Мелкие правки в справке.

2014-09-15-0:
Добавлен перевод новых пунктов в английскую справку, спасибо  Kildor'у
за помощь в переводе.

2014-09-13-0:
Обнаружилось, что в конечный список попадают имена  подкаталогов,  что
огорчало и заставляло их удалять вручную. Пришлось написать  отдельную
процедуру составления списка файлов в каталогах и добавить пару  опций
для регулирования добавления имен каталогов в конечный список.  Теперь
добавление имен каталогов в список регулируется.

Добавлена опция "Не включать в список каталоги с файлами"

Добавлена опция "Не включать в список пустые каталоги"

П.С.: Т.к. с англицким у меня плохо, прошу помочь  знатоков  перевести
пункты справки  касаемые  этих  двух  опций.  Ну  и  в  языковый  файл
заглянуть, правильно ли я перевел на английский названия этих пунктов.

2014-09-11-0:
Добавлен скрипт и краткая инструкция по сборке динамических библиотек
плагина.

2014-09-10-1:
Заменены DLL-ки на файлы скомпилированные мною,  спасибо  Shmuel'у  за
помощь в постижении сей науки.

2014-09-10-0:
Заменены DLL-ки на файлы из плагина LuaMacro, они более функциональны,
чем старые.

Изменены имена файлов, плагин обновлять лучше  вручную  удалив  старые
файлы.

Небольшой рефакторинг.

2014-09-08-0:
Поправлено описание (Description) плагина в _globalinfo.lua.

Добавлено поддержка значения %C (количество файлов) в формат строки.

Добавлена поддержка полей %tT и %tY во все три шаблона (шапка, текущий
файл, подвал) отчета. Описание полей:

           %tT          Текущее время (hh:mm:ss)
           %tY          Текущая дата (YYYY.MM.DD)

    дата и время при  этом  генерируется  в  начале  каждой  генерации
    списка (т.е. генерируется сразу после нажатия кнопки "Создать")  и
    остаётся неизменным пока список не будет сформирован и выведен.

Чуток подправлена и дополнена справка.

2014-09-07-1:
Поправлена версия плагина в _globalinfo.lua.

Убрана версия плагина из справки, дабы не править в нескольких местах.

2014-09-07:
Поправлена минимальная версия в _globalinfo.lua.

2014-09-06:
Окончательный релиз.
